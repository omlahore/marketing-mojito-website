# SEO fix sweep — progress log (2026-07-02, session 2)

One line per change + verification result. Baseline = audit.md §8 (do-not-redo list).
All items below built (`npm run build` ✓ 109/109 pages), deployed via `./deploy-lightsail.sh`, and verified against https://marketingmojito.com with curl.

## Environment / recon
- DataForSEO probe: `40201` still frozen → all [DATA] tasks parked (keyword volume validation, content gap, disavow generation). Unblock path: owner emails support@dataforseo.com from om@marketingmojito.com (audit.md §3.3).
- Webflow cruft review: badge code in `public/js/webflow.js` is dormant (no `data-wf-status` attr on any page → never injected) — left. Darkreader CDN script = deliberate site dark-mode toggle feature — left. `data-wf-*` attrs are required by webflow.js interactions — left. **No** `webflow.io`/staging/wp-content references exist in `public/*.html`. External CDN scripts (finsweet, gsap) all return 200.

## Changes shipped & verified
1. **Dead WP images stripped** — 41 posts in `content/blog-posts.json` had `<img>` tags pointing to dead `marketingmojito.com/blog/wp-content/...` URLs (live 404s). All removed (empty figures/paras cleaned). Remaining `wp-content` refs: 69 featuredImage fields (renderer already falls back to `/images/42.png`) + 1 legit outbound citation link. *Verified:* stripped post live, `grep -c wp-content` = 0.
2. **Title trim** — `mojito-labs.html` (63→48 chars) and `portfolio.html` (62→43) `<title>` trimmed to ≤60 rendered chars. Other 3 flagged titles were ≤59 rendered (entity-encoding artifact) — left. *Verified live.*
3. **Commercial SEO copy sections on 8 service pages** (audit §5.1): `growth-marketing-seo-content-services` (SEO services in India), `paid-advertising` (PPC services), `performance-analytics-cro-services` (performance marketing/CRO), `brand-visual-identity` (branding agency), `motion-animation`, `photography-videography` (video production company), `websites-ecommerce` (website development company), `strategy-management`. Each: keyword-bearing `<h2>`, ~600–900 words of genuinely useful copy (what's included / process / why us), 4–5 FAQs + **FAQPage JSON-LD**, internal links to sibling services, CTA to `/contact-us`. Brand hero H1s untouched. Inserted before the helpline/contact CTA section using existing Webflow classes (no animated `data-w-id` styles → no opacity traps). *Verified live:* each page 1×`<h1>`, 4×`application/ld+json` (Org+Breadcrumb+Service+FAQ), section present, HTML tag-balance OK.
4. **Homepage keyword targeting** — `app/HomeContent.tsx`: "Who We Are" para now names "full-service digital marketing agency in India"; new keyword H2 section ("Digital Marketing Services That Grow Businesses Across India") with links to all service + industry pages and CTA. Brand hero H1 untouched (title tag already keyword-targeted). *Verified live.*
5. **New page `/social-media-marketing`** — App Router, PageShell pattern (mirrors celebrity-brand-marketing): full metadata, Service + FAQPage JSON-LD, 6 FAQs, services grid cross-linking paid/branding/viral/video, process, CTAs. Owns "social media marketing agency/services" (was unowned per audit §5.1). *Verified live: 200, correct H1.*
6. **4 city landing pages** — `/digital-marketing-agency-{mumbai,delhi,bengaluru,pune}` via shared `components/CityLandingPage.tsx`. Unique per-city copy (hero, why-us paras, industry grids, 4 FAQs each), Service JSON-LD with `areaServed: City`, FAQPage JSON-LD. **No fabricated NAP** — no addresses invented. *Verified live: all 200, 3 JSON-LD each.*
7. **Blog internal-linking engine** — `lib/blog.ts` `getRelatedPosts()` (title-token + category scoring, deterministic); `app/blog/[slug]/page.tsx` renders "Related articles" block (3 links) on every post → sitewide crawlable cross-links without touching 73 JSON bodies. `components/BlogCta.tsx` now matches on title+categories and routes social posts to `/social-media-marketing`. *Verified live on old + new posts.*
8. **12 new cluster blog posts** (all with metaDescription, 5 FAQs → FAQPage rich results, h2-led structure, service-page + sibling-post internal links, valid local featuredImage): choose-agency guide, digital marketing cost India, SEO vs PPC, local SEO India guide, small-business social strategy, Instagram Reels guide, Google vs Facebook Ads India, website cost India, branding vs marketing, e-commerce marketing India, video marketing guide, AI in digital marketing (incl. AEO section). Commercial + informational intent, cross-linked into topic clusters, priced-content angle for AI answer engines. *Verified live: all 12 return 200 with FAQ schema; internal blog hrefs validated against slug set (0 broken).* Total posts 73→85.
9. **Sitemap** — 5 new routes added to `app/sitemap.ts`; new posts picked up automatically. *Verified live: sitemap.xml has 127 URLs incl. all new pages.*

## Skipped / parked (with reason)
- [DATA] keyword-volume validation, content-gap run, disavow generation — DataForSEO frozen (40201). Runbook unchanged in audit.md §3.3/§5.2.
- [OFF-SITE] Webflow staging mirror de-indexing, Google Business Profile creation, GSC sitemap submission — owner actions (audit.md §5.2a).
- Mass metaDescription/faqs backfill for the other ~66 legacy posts — renderer falls back to excerpt; lower leverage than new commercial content. Candidate for next session.
- Industry pages (healthcare/real-estate/saas/hospitality/entertainment) got no new copy section this pass — same generator (`scratchpad/seo_sections_lib.py` pattern) can be reused.
- `render_blocking_resources` / `low_content_rate` — inherent to Webflow export per audit §5.4; copy expansion already improves content rate on the 8 core pages.

## Next highest-leverage moves
1. Owner: kill the `marketing-mojit.webflow.io` mirror (Webflow dashboard) + submit sitemap in GSC — removes ~75% backlink noise, zero code.
2. Owner: create Google Business Profile(s) — biggest local-ranking lever, can't be done in code.
3. Unblock DataForSEO → validate keyword volumes, run content gap vs real agencies (webchutney/socialbeat/schbang), generate disavow.
4. Extend copy sections to the 5 industry pages; backfill FAQs/metas on top legacy posts.

## Post-review fix (same day)
- Homepage SEO section reformatted after user feedback (didn't match site layout — narrow left block, dead space right). Now a responsive two-column grid: `heading-style-h2` "Digital Marketing Services" left, copy + CTA right, full container width, stacks on mobile. CTA label shortened to "Book a Free Call" (arrow icon collided with longer label). Verified in preview (desktop 1440 + mobile) and live post-deploy.
- Second pass on same section: root cause found — site CSS sets `white-space: nowrap` on `.heading-style-h2`, so the heading rendered as one unwrapped line overlapping the copy column. Fixed: nowrap overridden (scoped), heading now full-width row, copy in two balanced columns below with CTA. Verified in preview (1440 + 375 viewports; mobile para truncation is the site's own who-para clamp, consistent with "Who We Are") and live post-deploy.
- Mobile audit (402px, iPhone-width) across homepage, service page, blog post, city page, about-us with a JS overflow detector. Real bugs found only on homepage: (a) Webflow CSS `white-space: nowrap` on section labels (.hero_header-left "Who We Are", .hero_header-left-2 "OUR SOLUTIONS", .cards_text-left "PORTFOLIO") made them wider than the phone screen and shifted blocks to negative x; (b) IX2 scroll-parallax slides PORTFOLIO/CLIENT SUCCESS labels ±100px → off-screen at phone widths. Fix: new `public/css/mobile-fixes.css` (wrap labels + freeze label parallax ≤767px), linked in WebflowStyles.tsx (Next layer) and injected into all 35 static HTML heads. Note: `section-center.css` exists but is loaded by nothing (orphan) — left as-is. Verified in preview at 402px (all labels in-viewport, overflow re-scan clean) and live (CSS 200, linked on both layers).
